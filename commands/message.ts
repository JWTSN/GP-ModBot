module.exports = {
    name: 'message',
    description: 'I use this to look through the message return, it will be deleted after initial development is over.',
    guildOnly: true,
    execute(message, args) {
    console.log(message);
    },
};
